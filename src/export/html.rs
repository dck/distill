use std::path::Path;

use crate::error::Result;

pub fn export_html(
    _content: &str,
    _title: Option<&str>,
    _output_path: Option<&Path>,
) -> Result<()> {
    todo!("HTML export not yet implemented")
}
